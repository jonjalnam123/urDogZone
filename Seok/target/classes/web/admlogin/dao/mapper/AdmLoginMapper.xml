<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="web.admlogin.dao.face.AdmLoginDao">

	<select id="adminLogin" parameterType="AdminDTO" resultType="AdminDTO">
		/* LoginDao.adminLogin */
	    SELECT ADMIN_ID
	    		, ADMIN_DEL_YN
	    		, ADMIN_TYPE
	    		, REG_ID
	    		, REG_DT
	    		, UPD_ID
	    		, UPD_DT
	      FROM TB_ADMIN_USER_INFO
	     WHERE ADMIN_ID = #{adminId}
	        AND ADMIN_PW = #{adminPw}
	        AND ADMIN_DEL_YN = 'N'
	</select>

</mapper>